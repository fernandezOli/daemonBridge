{
  "name": "daemon-bridge",
  "version": "0.1.0-mono-alpha",
  "description": "Daemon for bridge.",
  "author": "Olivier F. and Julien B.",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "deasync": "^0.1.30",
    "dotenv": "^16.4.5",
    "ethers": "^5.7.2",
    "fs": "^0.0.1-security",
    "http": "^0.0.1-security",
    "nodemailer": "^6.9.16",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "node daemonBridge.js",
    "build": "react-scripts build",
    "test": "node daemonBridge.js",
    "eject": "react-scripts eject",
    "sepolia": "node daemonBridge.js -p:3100 -c:./config/sepolia_min.json",
    "optimism": "node daemonBridge.js -p:3200 -c:./config/optimism_min.json",
    "daemon1": "node daemonBridge.js -p:3101 -c:./config/sepolia_min.json",
    "daemon2": "node daemonBridge.js -p:3102 -c:./config/sepolia_min.json",
    "daemon3": "node daemonBridge.js -p:3103 -c:./config/sepolia_min.json",
    "daemon4": "node daemonBridge.js -p:3104 -c:./config/sepolia_min.json",
    "julien": "node daemonBridge.js -p:3001 -c:./config/julien.json"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "hardhat": "^2.22.15"
  }
}
